import { MemberPartialPipe } from './member-partial-pipe';

describe('MemberPartialPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberPartialPipe();
    expect(pipe).toBeTruthy();
  });
});
