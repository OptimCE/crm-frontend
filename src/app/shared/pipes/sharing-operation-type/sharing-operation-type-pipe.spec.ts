import { SharingOperationTypePipe } from './sharing-operation-type-pipe';

describe('SharingOperationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SharingOperationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
