import { MapNumberStringPipe } from './map-number-string-pipe';

describe('MapNumberStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MapNumberStringPipe();
    expect(pipe).toBeTruthy();
  });
});
